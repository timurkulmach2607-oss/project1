- У `utils.py` додано нову функцію `generate_return_act_docx(asset, old_assigned_to)`, яка формує документ, де користувач передає майно компанії.
- У `app.py` `render_scanning` додано обробку кнопки "🔙 Зняти з користувача (Повернути на склад)".
- Додано можливість завантажити "Акт повернення обладнання".
- При натисканні кнопки актив автоматично змінює статус на "В наявності (In Stock)", скидає прив'язку (`assigned_to = 'Не призначено'`) та записує цю дію в журнал аудиту.
