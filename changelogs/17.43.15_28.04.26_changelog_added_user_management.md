- В `database.py` додано функції для керування користувачами (get_all_users, add_user, delete_user).
- В `app.py` додано нову вкладку "⚙️ Налаштування", яка відображається лише для адміністратора (`admin`).
- Реалізовано форму реєстрації нових користувачів із хешуванням пароля та записом в аудит.
- Реалізовано видалення користувачів (окрім головного `admin`).
