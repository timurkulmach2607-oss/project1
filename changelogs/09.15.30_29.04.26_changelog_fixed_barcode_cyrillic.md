- Виявлено помилку: бібліотека Code128 падає при спробі згенерувати штрихкод з кириличними символами (наприклад, `НОУ-12345`).
- В `app.py` замінено логіку генерації `barcode_data`: тепер замість перших трьох літер кириличної категорії використовуються фіксовані латинські префікси (`LAP`, `PC`, `PRN`, `MON`, `NET`, `OTH`).
- Написано та запущено скрипт `fix_db.py`, який оновив існуючі записи в базі даних, виправивши їх штрихкоди на англійські, щоб сканер працював без помилок.
