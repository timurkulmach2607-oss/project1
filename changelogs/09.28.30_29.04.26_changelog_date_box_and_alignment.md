- В `utils.py` реалізовано поміщення дати у таблицю 1x1 з видимими рамками, щоб вона виглядала як прямокутник (з вирівнюванням по правому краю).
- Збільшено кількість перенесень рядка (`\n`) для блоку підпису з боку користувача з 6 до 8, щоб візуально опустити лінію підпису на той же рівень, де знаходиться підпис компанії.
