- Додано створення таблиці (ALTER TABLE) з підтримкою `photo_path` в `database.py`.
- Оновлено логіку вставки (`INSERT`) з підтримкою `photo_path`.
- Створено локальну директорію `uploads/`.
- В `app.py` на форму реєстрації додано `st.file_uploader` для завантаження фотографій.
- В `app.py` на вкладці ідентифікації додано рендер завантаженої фотографії приладу, якщо вона існує.
