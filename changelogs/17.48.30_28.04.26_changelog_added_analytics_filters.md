- Додано `plotly` до `requirements.txt`.
- В `app.py` на вкладці Дашборд додано кругову та стовпчикову діаграми.
- На Дашборд додано зручні фільтри (за категорією, статусом та пошук по тексту), які динамічно впливають на таблицю та експорт в CSV.
